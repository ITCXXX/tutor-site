# Файлы браузерного питона (Pyodide) — двоичные: сам питон в WebAssembly,
# архивы стандартной библиотеки и колёса пакетов. У репозитория включена
# подмена переводов строк (core.autocrlf), и без этой строки git может
# «поправить» их внутри двоичного файла. Сломанный .wasm или .whl не откроется,
# а заметим мы это только на сервере, на уроке.
vendor/pyodide/** binary
