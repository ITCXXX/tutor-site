# -*- coding: utf-8 -*-
"""
quoridor/bot.py — соперник для «Заборов» на стороне сервера.

Зачем он, если бот уже есть
---------------------------
Играющий бот живёт в браузере (static/quoridor/js/bot.js): локальная партия
целиком браузерная, и правила там уже лежат. Этот модуль его НЕ заменяет — он
нужен, чтобы бота можно было ИЗМЕРЯТЬ и НАСТРАИВАТЬ.

Никакая настройка невозможна, пока «стало сильнее» проверяется на глаз. Для
крестиков-ноликов это уже стоило одного неверного вывода: перебор на глубину 6
казался сильнее, а на замерах проигрывал. Стенд (quoridor/arena.py) играет
сотни партий бот против бота и говорит, значима ли разница; но стенд не может
работать в браузере — там нет ни пятнадцати ядер, ни возможности прогнать
тысячу партий за минуту.

Поэтому здесь ровно то же, что в bot.js, только на Python: тот же отбор ходов,
тот же перебор, та же оценка. Настроенные числа возвращаются в bot.js —
переезжают именно ЧИСЛА, а не устройство.

Чем этот порт рискован и что с этим сделано
-------------------------------------------
Две реализации одного и того же всегда расходятся. В «Заборах» это уже
учтено для правил: `manage.py quoridor_trace` пишет след серверной партии, а
`tracecheck.js` проигрывает его браузерными правилами. Здесь тот же приём
распространён на бота — след умеет хранить и выбор хода, так что расхождение
видно сразу, а не в партии с игроком.

Оценка
------
Она в «Заборах» на удивление простая и точная: разность длин кратчайших путей
плюс небольшая надбавка за оставшиеся заборы. Побеждает тот, кому осталось
меньше шагов, а заборы — ресурс, чтобы шагов у соперника стало больше.

Числа вынесены в ВЕСА не для красоты: их всего два, и в bot.js они вписаны
прямо в формулу, поэтому подобрать их там нечем.
"""

import random
import time

from .engine import (BLUE, RED, apply_move, apply_wall, distance_map, other,
                     path_to, pawn_moves, размер, сетка_заборов, shortest_path,
                     wall_conflict)

WIN = 10000

# Уровни повторяют bot.js. Срок указан в секундах (в JS — в миллисекундах):
# на сервере счёт времени секундами принят везде, и переводить его туда-сюда
# опаснее, чем один раз записать иначе.
LEVELS = {
    'easy': {
        'title': 'слабый',
        'depth': 1,            # видит только свой ход
        'candidates': 6,
        'budget': 0.10,
        'blunder': 0.25,       # как часто ошибается нарочно
        'wall_need': 1,
    },
    'medium': {
        'title': 'средний',
        # Было 2. Стенд: глубина 3 обыгрывает глубину 2 со счётом 67.9% ± 3.2%
        # (800 партий на семёрке) — разница громадная и доказанная. На боевой
        # девятке ход занимает 0.16 с при сроке 0.30, то есть уровень
        # досчитывается целиком, и выигрыш доезжает до сайта, а не остаётся
        # в замере.
        'depth': 3,            # свой ход, ответ соперника и свой ответ на него
        'candidates': 10,
        'budget': 0.30,
        'blunder': 0.05,
        'wall_need': 1,
    },
    'hard': {
        'title': 'сильный',
        'depth': 4,            # по два хода с каждой стороны
        'candidates': 14,
        'budget': 0.90,
        'blunder': 0.0,
        'wall_need': 1,
    },
}

DEFAULT_LEVEL = 'medium'
ALL_LEVELS = tuple(LEVELS)

# Цена каждого признака оценки. Их два, и для выбора хода важно только их
# отношение: умножь оба на что угодно — ходы не изменятся.
#
# Забор стоит РОВНО шага, и это подобрано замерами, а не выведено из красоты.
# Раньше он стоил двойку, то есть пятую часть шага, и это оказалось дорогой
# ошибкой. Бот отбрасывает заборы, отыгрывающие меньше одного шага; при цене 2
# забор, отнимающий у соперника ровно шаг, оценивался как +10 - 2 = +8, то есть
# выгодно. Бот разбазаривал заборы за один шаг, а вернуть их нельзя. При цене 10
# тот же забор даёт ноль, и он ставится, только если отнимает два шага и больше.
#
# Замеры на поле 9x9 (quoridor/study.txt, manage.py quoridor_study):
#   перебор цен против прежней двойки — вершина на плато 10..14, около 61%;
#   круговой турнир 2/7/10/14 — 10 берёт 56.8% очков, 7 берёт 46.2%, 2 берёт
#     41.0%; десятка обыгрывает семёрку и в очной встрече (58.5%);
#   проверка на СИЛЬНОМ уровне, 240 партий — 63.3% ± 6.1% против прежней.
#
# Вершина зависит от размера поля: на 5x5 её почти нет (партия идёт 16
# полуходов, запас заборов не успевает пригодиться), на 7x7 и 9x9 выражена.
# Внутри плато 10..14 разницы нет, поэтому взята десятка — при ней правило
# читается одной фразой: забор стоит шага.
ВЕСА = {
    'шаг': 10.0,      # каждый шаг, на который соперник дальше от цели, чем я
    'забор': 10.0,    # каждый лишний забор в запасе — столько же, сколько шаг
}

# ЧТО СИЛЬНОМУ УРОВНЮ НЕ ПОМОГЛО. Все замеры — бот против такого же бота,
# отличается одна настройка; девятка, если не сказано иное.
#
#   глубина 5 вместо 4        51.4% ± 3.5% (семёрка, 800) и 52.0% ± 4.9% (400)
#   срок 1.8 с вместо 0.9     54.6% ± 6.3% (240)
#   срок 0.45 с вместо 0.9    47.9% ± 6.3% (240)
#   кандидатов 20 вместо 14   47.5% ± 6.9% (200)
#   глубже корня 4 забора     49.4% ± 6.3% (240) и 53.6% ± 4.9% (семёрка, 400)
#
# Ни одной значимой разницы. Вывод не «бот идеален», а «упор не в перебор»:
# силе безразличны и глубина, и время от 0.45 до 1.8 с, и ширина списка ходов.
# Значит, следующий шаг — оценка позиции, и время на неё есть.
#
# Но и первая попытка улучшить оценку провалилась, причём громко. Признак
# «простор» (сколько ходов фишки ведут строго ближе к цели: дорога шириной в
# одну клетку рвётся одним забором, а в две-три — держит удар) дал на девятке
# 35.4% ± 6.1% при весе 3 и 34.6% ± 6.0% при весе 8, то есть минус пятнадцать
# процентов; на семёрке — вничью. Похоже, он платит за простор продвижением:
# бот выбирает стоять там, где много дорог, вместо того чтобы идти. Признак
# убран, замер оставлен здесь.
#
# СРОК УМЕНЬШАТЬ НЕЛЬЗЯ, хотя замер и разрешает: 0.45 с против 0.9 с дали
# ничью. Срок — это ПОТОЛОК, и на быстром компьютере он не мешает, а на слабом
# телефоне бот в него упирается. Урезав его, мы ослабим бота ровно там, где он
# и так слабее всего.
#
# Проверено и отвергнуто: поправка на прыжок в оценке.
#
# Точное решение маленьких полей (quoridor/strategy.py) показало, что вся игра
# держится на прыжке: вне одного столбца это чистая гонка по длине пути, а в
# столбце всё решает чётность расстояния, потому что подошедший вплотную дарит
# сопернику два ряда за ход. При этом shortest_path считает дорогу по заборам и
# фишку соперника НЕ ВИДИТ вовсе — казалось, оценке не хватает ровно этого.
#
# Оказалось, хватает. Прыжок случается через один-два полухода, и перебор видит
# его сам. Бот с поправкой против такого же без неё, одинаковые уровни:
#     пятёрка, сильный   50.3% ± 2.8%  (1200 партий)
#     семёрка, сильный   49.8% ± 4.9%  ( 400 партий)
#     пятёрка, средний   50.6% ± 1.8%  (3000 партий)
#     пятёрка, слабый    49.9% ± 1.8%  (3000 партий)
# Ни одной значимой разницы почти на 7800 партиях. Поправка убрана: выключенный
# вес в оценке через полгода примут за нужный и начнут «настраивать».


def level_of(name):
    """Уровень по имени — или готовый набор настроек как есть.

    Словарь принимается ради разбора игры: там нужны уровни, которых на сайте
    нет и быть не должно (глубина 1..6 при снятом сроке). Заводить их в LEVELS
    нельзя — оттуда они попали бы в выбор игрока.
    """
    if isinstance(name, dict):
        return name
    return LEVELS.get(name, LEVELS[DEFAULT_LEVEL])


def _key(r, c):
    return '%d,%d' % (r, c)


# ────────────────────────── оценка ──────────────────────────

def distances(state, side):
    """(мой путь, путь соперника) в шагах. None, если пути нет."""
    foe = other(side)
    n = размер(state)
    return (shortest_path(state['walls'], state['pawns'][side],
                          state['goalRow'][side], n),
            shortest_path(state['walls'], state['pawns'][foe],
                          state['goalRow'][foe], n))


def evaluate(state, side, веса=None):
    """Оценка позиции глазами стороны `side`. Больше — лучше для неё."""
    if state['winner']:
        return WIN if state['winner'] == side else -WIN
    в = веса if веса is not None else ВЕСА
    мой, чужой = distances(state, side)
    if мой is None or чужой is None:          # такого быть не должно
        return 0.0
    foe = other(side)
    return ((чужой - мой) * в['шаг']
            + (state['wallsLeft'][side] - state['wallsLeft'][foe]) * в['забор'])


# ────────────────────── ходы-кандидаты ──────────────────────

def wall_candidates(state, side, limit):
    """Заборы, перекрывающие сопернику его нынешний кратчайший путь.

    Полный перебор невозможен: мест под забор 128, и уже на третьем полуходе
    это миллионы позиций. Но забор, поставленный в стороне от дороги, соперник
    обойдёт не заметив — считать такие варианты значит тратить перебор впустую.
    Поэтому берутся только те, что режут текущий путь: полтора десятка вместо
    ста двадцати восьми.
    """
    if state['wallsLeft'][side] <= 0:
        return []
    foe = other(side)
    n = размер(state)
    сетка = сетка_заборов(n)
    путь = path_to(state['walls'], state['pawns'][foe], state['goalRow'][foe], n)
    if not путь or len(путь) < 2:
        return []

    def зажать(x):
        return max(0, min(сетка - 1, x))

    видели = set()
    из = []
    мой_базовый, чужой_базовый = distances(state, side)

    for i in range(len(путь) - 1):
        a, b = путь[i], путь[i + 1]
        места = []
        if b['r'] != a['r']:                  # шаг по вертикали режет забор «—»
            wr = min(a['r'], b['r'])
            места = [(wr, зажать(a['c'] - 1), 'h'), (wr, зажать(a['c']), 'h')]
        else:                                 # шаг вбок режет забор «|»
            wc = min(a['c'], b['c'])
            места = [(зажать(a['r'] - 1), wc, 'v'), (зажать(a['r']), wc, 'v')]

        for wr, wc, kind in места:
            метка = (wr, wc, kind)
            if метка in видели:
                continue
            видели.add(метка)
            # Дешёвые проверки — очередь, запас, края, нахлёст. Правило
            # «забор не отрезает дорогу» проверять отдельным обходом поля не
            # нужно: ниже всё равно считаются оба пути, и None означает ровно
            # отрезанную дорогу. Было четыре обхода на забор вместо двух.
            if wall_conflict(state, side, wr, wc, kind):
                continue

            проба = dict(state['walls'])
            проба[_key(wr, wc)] = kind
            чужой = shortest_path(проба, state['pawns'][foe],
                                  state['goalRow'][foe], n)
            мой = shortest_path(проба, state['pawns'][side],
                                state['goalRow'][side], n)
            if чужой is None or мой is None:
                continue
            из.append({
                'kind': 'wall', 'wr': wr, 'wc': wc, 'orient': kind,
                'gain': (чужой - чужой_базовый) - (мой - мой_базовый),
            })

    из.sort(key=lambda x: -x['gain'])
    return из[:limit]


def pawn_candidates(state, side):
    """Шаги фишкой, отсортированные по близости к цели.

    Длины берутся из одной карты расстояний, а не из обхода поля на каждый
    ход. Это тот же ответ: расстояние от клетки до целевого ряда не зависит от
    того, с какой стороны его считать. А обходов на узел становится на четыре
    меньше — при сильном уровне, который упирается в срок, это прямая глубина.
    """
    цель = state['goalRow'][side]
    n = размер(state)
    карта = distance_map(state['walls'], цель, n)
    из = []
    for m in pawn_moves(state, side):
        длина = карта.get((m['r'], m['c']))
        if длина is None:
            continue
        из.append({'kind': 'move', 'r': m['r'], 'c': m['c'], 'len': длина})
    из.sort(key=lambda m: m['len'])
    return из


def candidates(state, side, limit):
    """Ходы к перебору: сначала шаги покороче, потом заборы повреднее."""
    return pawn_candidates(state, side) + wall_candidates(state, side, limit)


def _применить(state, side, ход):
    if ход['kind'] == 'move':
        новое, ошибка = apply_move(state, side, ход['r'], ход['c'])
    else:
        новое, ошибка = apply_wall(state, side, ход['wr'], ход['wc'],
                                   ход['orient'])
    return None if ошибка else новое


# ────────────────────────── перебор ──────────────────────────

class _Срок:
    """Часы, а не счётчик позиций.

    Считать позиции здесь бесполезно: время съедает не сам перебор, а обходы
    поля при отборе заборов. Часы меряют то самое, ради чего потолок и нужен.
    """

    __slots__ = ('до',)

    def __init__(self, сколько):
        self.до = time.monotonic() + сколько

    def вышел(self):
        return time.monotonic() > self.до


def search(state, side, depth, alpha, beta, level, срок, веса):
    """Негамакс с отсечениями. Оценка глазами того, кто ходит."""
    if state['winner']:
        # depth — ОСТАТОК глубины: у победы поближе он больше. Поэтому близкая
        # победа обязана стоить дороже далёкой, иначе бот в шаге от выигрыша
        # предпочтёт выигрыш «через два хода» и будет топтаться.
        return (WIN + depth) if state['winner'] == side else -(WIN + depth)
    if depth <= 0 or срок.вышел():
        return evaluate(state, side, веса)

    лучшее = None
    for ход in candidates(state, side, level['candidates']):
        if срок.вышел():
            break
        следом = _применить(state, side, ход)
        if следом is None:
            continue
        очки = -search(следом, other(side), depth - 1, -beta, -alpha,
                       level, срок, веса)
        if лучшее is None or очки > лучшее:
            лучшее = очки
        if лучшее > alpha:
            alpha = лучшее
        if alpha >= beta:
            break                              # соперник сюда не пойдёт
    return evaluate(state, side, веса) if лучшее is None else лучшее


# ───────────────────────── ход бота ─────────────────────────

def choose_move(state, side, level_name=DEFAULT_LEVEL, rng=None, веса=None):
    """Выбрать ход. Возвращает словарь хода или None, если ходить нечем."""
    level = level_of(level_name)
    rng = rng or random
    список = candidates(state, side, level['candidates'])
    if not список:
        return None

    стоящие = [х for х in список
               if х['kind'] != 'wall' or х['gain'] >= level['wall_need']]
    пул = стоящие or список

    срок = _Срок(level['budget'])
    оценённые = _углубляясь(state, side, пул, level, срок, веса)

    if not оценённые:
        шаг = next((х for х in список if х['kind'] == 'move'), None)
        return шаг

    оценённые.sort(key=lambda п: -п[1])

    # Слабый уровень нарочно ошибается: иначе даже одноходовый бот обыгрывает
    # новичка вчистую, и играть с ним не хочется.
    if level['blunder'] and len(оценённые) > 1 and rng.random() < level['blunder']:
        return оценённые[1 + rng.randrange(len(оценённые) - 1)][0]

    лучшая = оценённые[0][1]
    верхние = [х for х, о in оценённые if о == лучшая]
    if len(верхние) > 1:
        верхние = _ближе_к_цели(state, side, верхние)
    return верхние[rng.randrange(len(верхние))]


def _ближе_к_цели(state, side, ходы):
    """Из равных по оценке ходов оставить те, что ближе подводят к своему краю.

    Это лечение буриданова осла. Замер: на среднем уровне в 2-6% решений первое
    место делят ходы, ведущие в РАЗНЫЕ стороны — один приближает бота к цели,
    другой отдаляет. Оценка их не различает, и бот бросал монетку; со стороны
    это выглядит метанием — шаг вперёд, шаг назад. Партию он всё равно
    выигрывал, поэтому в самоигре беда не видна: она портит не результат, а
    впечатление, и именно её игрок и заметил.

    На слабом уровне такого не бывает: там глубина 1, и оценка прямо видит
    расстояние. Ничьи появляются с глубины 2, когда в счёт входит ответ
    соперника и разница между «вперёд» и «назад» успевает сравняться.

    Случайность при этом не теряется: выбор остаётся случайным среди тех, кто
    одинаково хорош И по оценке, И по продвижению. Заборы не двигают фишку,
    поэтому попадают в ту же корзину, что ходы, не меняющие расстояния.
    """
    n = размер(state)
    цель = state['goalRow'][side]
    сейчас = shortest_path(state['walls'], state['pawns'][side], цель, n)
    if сейчас is None:
        return ходы

    лучшее = None
    с_ценой = []
    for ход in ходы:
        следом = _применить(state, side, ход)
        if следом is None:
            continue
        после = shortest_path(следом['walls'], следом['pawns'][side], цель, n)
        if после is None:
            continue
        с_ценой.append((ход, после))
        if лучшее is None or после < лучшее:
            лучшее = после
    if not с_ценой:
        return ходы
    return [ход for ход, после in с_ценой if после == лучшее]


def _углубляясь(state, side, пул, level, срок, веса):
    """Считать глубину 1 целиком, потом 2 целиком, и так пока есть время.

    Раньше было иначе: каждый ход считался сразу на полную глубину, и по
    истечении срока список ОБРЫВАЛСЯ. Беда не в том, что бот думал меньше, а в
    том, что он терял: ходы идут не вперемешку — сначала шаги фишкой, потом
    заборы по убыванию пользы, — и обрыв съедал с конца, то есть заборы. Замер
    на девятке при сильном уровне: срок обрывал список в 75% решений, теряя по
    три с лишним забора на ход. Бот превращался в бегуна не по расчёту, а
    потому что до заборов не доходила очередь.

    Замеры после правки: на сильном уровне 58.8% ± 7.7%, на среднем 51.7% ±
    3.5% и на слабом 51.5% ± 3.5% — то есть выигрыш там, где срок жмёт, и
    ничего не стоит там, где его хватает.

    Так список ходов всегда сравнён ПОЛНОСТЬЮ — пусть на меньшей глубине, но
    целиком, и заборы не пропадают из-за очереди. Недосчитанный уровень
    выбрасывается целиком: его оценки сравнивать не с чем.

    Побочная выгода — порядок. Каждый следующий уровень начинается с лучших
    ходов предыдущего, а чем раньше найден хороший ход, тем больше веток
    отсечёт альфа-бета.

    ОТСЕЧЕНИЯ В КОРНЕ. Внутри дерева окно сужается как положено, а здесь
    каждому ходу долго давалось полное окно -inf..+inf — то есть на верхнем
    уровне альфа-бета не работала вовсе, и заведомо худший ход досчитывался
    целиком. Теперь найденное лучшее значение передаётся вниз потолком для
    соперника: ход, который уже хуже, обрывается на первом же опровержении.

    ПЕРЕПРОВЕРКА ОБЯЗАТЕЛЬНА. Сужение окна даёт точную оценку только тем
    ходам, которые ХУЖЕ текущего лучшего. Ход, оказавшийся ЛУЧШЕ, обрывает
    перебор на первом же доказательстве своего превосходства и возвращает лишь
    нижнюю границу. Сравнивать такие границы между собой нельзя: из двух
    хороших ходов корень выберет тот, чья граница случайно оказалась выше.

    Это не рассуждение, а измеренная ошибка: без перепроверки бот проиграл
    сам себе 41.0% ± 4.8% на семёрке (400 партий) — то есть правка, которая
    «не меняет выбор хода», меняла его, и к худшему. Проверка на дюжине
    позиций этого не показала, потому что там лучший ход стоял первым и
    превышать было нечего.

    Поэтому ход, превысивший лучшего, считается заново полным окном. Отсечения
    остаются там, где вся выгода: на заведомо худших ходах, а их большинство.
    Равные лучшему возвращают ровно альфу и перепроверки не требуют, так что
    ничьи по оценке сохраняются и лечение буриданова осла (_ближе_к_цели)
    получает тот же список.
    """
    порядок = list(пул)
    готовое = []
    # Ключ уровня, а не вес: нужен тесту, который сверяет выбор хода с
    # прежним поведением. В боевых уровнях его нет — значит, отсечения всегда.
    сужать = level.get('корневое_окно', True)
    for глубина in range(1, level['depth'] + 1):
        круг = []
        прервано = False
        альфа = -float('inf')
        for ход in порядок:
            if срок.вышел() and готовое:
                прервано = True
                break
            следом = _применить(state, side, ход)
            if следом is None:
                continue
            узко = сужать and альфа != -float('inf')
            потолок = -альфа if узко else float('inf')
            очки = -search(следом, other(side), глубина - 1,
                           -float('inf'), потолок, level, срок, веса)
            # Пересчитываются и те, кто СРАВНЯЛСЯ с лучшим. Ход, который на
            # деле хуже, при узком окне возвращает верхнюю границу, и она
            # может оказаться ровно равной лучшему — тогда он попал бы в список
            # равных и участвовал в жеребьёвке наравне с настоящими. Замер
            # поймал ровно это: один выбор из ста расходился.
            if узко and очки >= альфа:
                очки = -search(следом, other(side), глубина - 1,
                               -float('inf'), float('inf'), level, срок, веса)
            if очки > альфа:
                альфа = очки
            круг.append((ход, очки))
        if прервано:
            break
        круг.sort(key=lambda п: -п[1])
        готовое = круг
        порядок = [ход for ход, _ in круг]
        if срок.вышел():
            break
    return готовое


def apply_choice(state, side, ход):
    """Применить выбранный ход. Возвращает (новое состояние, ошибка)."""
    if ход is None:
        return state, 'ходить нечем'
    if ход['kind'] == 'move':
        return apply_move(state, side, ход['r'], ход['c'])
    return apply_wall(state, side, ход['wr'], ход['wc'], ход['orient'])
